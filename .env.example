# Example environment variables for Personal Finance Tracker
# Copy this file to `.env` (not committed) and adjust values as needed.
# On Windows (cmd):  set JWT_SECRET=DevStrongSecretBase64
# On Windows (PowerShell):  $env:JWT_SECRET="DevStrongSecretBase64"
# On Linux/macOS:  export JWT_SECRET=DevStrongSecretBase64
#
# Generate a strong random 256-bit (32-byte) secret:
#   Linux/macOS: openssl rand -base64 32
#   PowerShell:  [Convert]::ToBase64String((1..32 | % { Get-Random -Max 256 } ))
#   Node.js:     node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
#
# NOTE: Do NOT use short or guessable strings for real deployments.

JWT_SECRET=wVt9lYb3XCo9P3absX5p9XWpc6tGpQW8cG2cJ7KxG9E=
# You can add others later, e.g. DB_URL, DB_USER, DB_PASSWORD
