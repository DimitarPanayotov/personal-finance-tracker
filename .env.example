# Environment Variables Template
# Copy this file to .env and set your own values

# Database password (default works out-of-the-box)
POSTGRES_PASSWORD=demo_password_123

# JWT Secret (optional, has secure fallback)
JWT_SECRET=fallbackSecretKeyThatIsSecureAndItIsLeast256BitsLongForSure

# Note: The application works without a .env file using these defaults
# Only create .env if you need custom credentials

